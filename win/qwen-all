Вот оптимизированный пошаговый план выполнения задания с учётом зависимостей, требований ИБ и минимизации рисков штрафов:

---

## 🔒 Критически важные ограничения (запомнить!)
- **Запрещено** использовать `reaskills2026.local` во внешней сети (только `rea26.ru`/`rea2026.ru`)
- **Запрещено** менять топологию или пробрасывать внутреннюю сеть наружу
- Все сертификаты для внешних сервисов — только с именем `*.rea26.ru` / `*.rea2026.ru`

---

## 📋 Пошаговый план выполнения

### Этап 0: Подготовка (5–10 мин)
1. Проверить связность между всеми серверами (`ping`, `nslookup`)
2. Убедиться, что все серверы присоединены к домену `reaskills2026.local`
3. Настроить статические IP согласно таблице адресации (если не сделано)
4. Проверить, что на `RRAS1` настроены интерфейсы для всех сетей (CORP/DMZ/EXTERNAL)

---

### Этап 1: Корпоративный ЦС на DC (15–20 мин) ⚠️ БАЗОВАЯ ЗАВИСИМОСТЬ
> Без этого не будут работать сертификаты для ADFS/WAP/RDS/HTTPS

1. Установить роль **Active Directory Certificate Services**
   - Тип: **Enterprise Root CA**
   - Имя: `REA2026-CA`
2. Настроить публикацию CRL:
   ```powershell
   certutil -setreg CA\CRLPublicationURLs "1:http://crl.rea26.ru/crl/%3%8.crl\n2:file://\\dc\certenroll\%3%8.crl"
   certutil -setreg CA\CACertPublicationURLs "1:http://crl.rea26.ru/cert/%3%8.crt\n2:file://\\dc\certenroll\%3%8.crt"
   ```
3. Создать папку `C:\inetpub\wwwroot\crl`, расшарить как `crl`
4. Настроить веб-сайт IIS на `DC` для публикации `http://crl.rea26.ru`:
   - Привязка к внешнему интерфейсу `RRAS1` через проброс порта 80
   - Добавить запись `crl.rea26.ru → 192.168.122.x` на `RRAS1` (внешний DNS)
5. Протестировать: `http://crl.rea26.ru` из внешней сети → должен отдавать `.crl` файл

---

### Этап 2: Подготовка сертификатов (10 мин)
> Заранее запросить все необходимые сертификаты через веб-интерфейс ЦС (`http://dc/certsrv`)

| Сервер | Имя в сертификате | Использование |
|--------|-------------------|---------------|
| ADFS   | `adfs.rea26.ru`   | SSL + аутентификация |
| WAP    | `rdweb.rea26.ru`  | SSL для прокси |
| RDS    | `rds.reaskills2026.local` | Внутренний SSL (только для внутренней сети!) |
| RDS    | `rds.rea26.ru`    | Для внешней публикации через WAP |

⚠️ **Важно:** Для `rds.reaskills2026.local` — только внутреннее использование. Для внешнего доступа — только `rdweb.rea26.ru` через WAP.

---

### Этап 3: Настройка ADFS (15 мин)
1. Установить роль **AD FS** на сервере `ADFS`
2. Настроить ферму:
   - Имя портала: `REA ADFS`
   - SSL-сертификат: `adfs.rea26.ru`
   - Учётная запись службы: встроенная `adfssrv`
3. Включить обязательную проверку CRL:
   ```powershell
   Set-AdfsProperties -EnableCRLCheck $true
   ```
4. Включить регистрацию:
   ```powershell
   Set-AdfsProperties -EnableLocalAuthenticationTypes $true
   Set-AdfsWebConfig -PageLayoutVersion 2
   ```
5. Протестировать: `https://adfs.rea26.ru/adfs/ls/idpinitiatedsignon.htm` → форма входа без ошибок

---

### Этап 4: Настройка WAP (15 мин)
1. Установить роль **Web Application Proxy** на `WAP`
2. Присоединить к ферме ADFS через мастер конфигурации
3. Опубликовать приложение:
   - Внешний URL: `https://rdweb.rea26.ru`
   - Внутренний URL: `https://rds.reaskills2026.local`
   - Аутентификация: **AD FS**
   - SSL-сертификат: `rdweb.rea26.ru`
4. Настроить аутентификацию по сертификату:
   - В консоли управления сертификатами импортировать корневой сертификат `REA2026-CA`
   - Выпустить пользовательский сертификат для тестового пользователя через `certmgr.msc`
   - Экспортировать с закрытым ключом → установить на `CLI-EXT`
5. Протестировать из внешней сети (`CLI-EXT`):
   - Открыть `https://rdweb.rea26.ru` → форма ADFS → вход по паролю или сертификату

---

### Этап 5: Настройка RDS (20 мин)
1. Установить мессенджер **MAX** (скачать с сайта, установить в режиме тихой установки при возможности)
2. Настроить веб-интерфейс только по HTTPS:
   - В IIS отключить привязку к порту 80
   - Или настроить редирект 301: `http → https` через правило URL Rewrite
3. Настроить **SSO для внутренней сети**:
   - В групповой политике для `CLI-INT` добавить `rds.reaskills2026.local` в **Интранет-зоны**
   - Настроить Kerberos Delegation для компьютера `RDS` → «Доверять для делегирования только указанным службам» → `HTTP/rds.reaskills2026.local`
4. Создать RDP-файл для приложения MAX:
   ```rdp
   full address:s:rds.reaskills2026.local
   prompt for credentials:i:0
   authentication level:i:2
   remoteapplicationmode:i:1
   remoteapplicationprogram:s:||MAX
   remoteapplicationname:s:MAX
   ```
5. Разместить ярлык через GPO:
   - `Конфигурация пользователя → Предпочтения → Windows → Ярлыки`
   - Путь: `%Public%\Desktop\MAX.lnk` → указать на `.rdp` файл
   - Фильтр: только для обычных пользователей (не администраторов)

---

### Этап 6: Групповые политики для Edge (10 мин)
1. Скачать и импортировать **административные шаблоны Edge** в `C:\Windows\PolicyDefinitions`
2. Создать/изменить GPO, применяемую к `OU=Workstations`:
   - `Конфигурация пользователя → Политики → Административные шаблоны → Microsoft Edge`
     - Стартовая страница: `https://rds.reaskills2026.local`
     - Отключить приветственное окно: **Включено**
     - Отключить анимацию первого входа: **Включено**
     - Убрать Дизн: настроить политику `HideFirstRunExperience` + `HideDefaultBrowserPrompt`
3. Применить политику: `gpupdate /force` на `CLI-INT`

---

### Этап 7: Финальное тестирование (15 мин)
| Тест | Из сети | Устройство | Ожидаемый результат |
|------|---------|------------|---------------------|
| Edge стартовая страница | Внутренняя | `CLI-INT` | Открывается `https://rds...` без приветствия |
| RDP-ярлык MAX | Внутренняя | `CLI-INT` | Запуск без пароля, без предупреждений |
| Веб-интерфейс RDS | Внутренняя | `CLI-INT` | HTTPS, SSO без запроса пароля |
| Веб-интерфейс RDS | Внешняя | `CLI-EXT` | `https://rdweb.rea26.ru` → форма ADFS → вход |
| RDP-файл из браузера | Внешняя | `CLI-EXT` | Скачивается → подключение с запросом пароля |
| Аутентификация по сертификату | Внешняя | `CLI-EXT` | Вход в ADFS/RDS без пароля |
| CRL | Внешняя | `CLI-EXT` | `http://crl.rea26.ru` → скачивается `.crl` |

---

## ⚠️ Типичные ошибки и как избежать штрафов
| Риск | Как избежать |
|------|--------------|
| Использование `.local` во внешней сети | Все внешние имена — только `*.rea26.ru` / `*.rea2026.ru` |
| Внутренняя сеть доступна извне | Не открывать порты напрямую с `CORP` в `EXTERNAL`. Только через `WAP` в `DMZ` |
| Предупреждения сертификатов | Все сертификаты — от `REA2026-CA`, корневой сертификат развёрнут через GPO |
| Требование пароля при внутреннем SSO | Проверить делегирование Kerberos + зону безопасности в браузере |
| Недоступность CRL | Проверить права на папку `certenroll`, привязки IIS, маршрутизацию через `RRAS1` |

---

## 🕒 Оценка времени
- Минимум: 90–100 минут (при хорошей подготовке)
- Рекомендуемый буфер: +20 минут на отладку CRL/SSO/сертификаты

Начните с **Этапа 1 (ЦС)** — это критический путь. Без корректной работы ЦС и CRL все остальные этапы будут провалены из-за ошибок сертификатов.
